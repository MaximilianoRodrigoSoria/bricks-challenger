CREATE TABLE product (
                         id INT AUTO_INCREMENT PRIMARY KEY,
                         name VARCHAR(255),
                         price DECIMAL(10, 2),
                         stock INT,
                         category INT
);